import { COARSE_POINTER_INPUT_HEIGHT_CLASS } from "../../components/ui/coarse-pointer-sizing";

/** Matches vendored BB Input / SelectTrigger (h-9, coarse h-10). Use on paired buttons. */
export const CONTROL_H = COARSE_POINTER_INPUT_HEIGHT_CLASS;
